<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/topicDropdownRoot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/txtTopicSubHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/select_topics_under_employment"
            android:textColor="@color/black"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <LinearLayout
            android:id="@+id/llTopicDropdown"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:background="@drawable/spinner_bh"
            app:layout_constraintStart_toStartOf="@id/txtTopicSubHeading"
            app:layout_constraintTop_toBottomOf="@id/txtTopicSubHeading">

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/txtSelectedValue"
                android:layout_width="match_parent"
                android:padding="8dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Select"
                app:drawableEndCompat="@drawable/drop_down"
                android:layout_height="30dp"
                android:textSize="12sp" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
